# Quy định thiết kế analysis class diagram

## A. Các bước thực hiện

Xuất phát từ use case specification. Và trích xuất theo từng UC riêng rồi merge lên module rồi merge lên toàn hệ thống.

### Bước 1: Trích xuất Boundary Class

Mỗi giao diện xuất hiện có tương tác với người dùng -> Đề xuất thành 1 Boundary Class
**Lưu ý:**

- Chỉ trích xuất các giao diện chính. Các giao diện như: Thông báo lỗi, thông báo thành công, xác nhận ok/cancel hay gom chung lại là các alert -> Bỏ qua, không trích xuất.
- Boundary Class thấp nhất luôn đặt cao hơn Entity Class cao nhất (nên như thế để dễ đọc diagram hơn chứ không bắt buộc phải tuân theo)
- Với trường hợp giao tiếp ứng dụng bên ngoài thì sẽ đặt tên là [Tên] + `API` để thể hiện rõ nó là API để Control Class xử lý.

**Naming Convention:**

- **Tên Lớp:** [Tên giao diện] + `View`. Viết theo PascalCase.
  - _Ví dụ:_ `LoginView`.

### Bước 2: Xác định thuộc tính cho Boundary Class

Với mỗi Boundary Class, xét các thông tin cần có để tương tác trên giao diện người dùng: Mỗi thông tin -> Đề xuất thành thuộc tính giao diện của lớp
**Lưu ý:**

- Thuộc tính để nhập dữ liệu vào: Dùng tiền tố "in\<attributeName>"
- Thuộc tính để hiển thị dữ liệu: Dùng tiền tố "out\<attributeName>"
- Thuộc tính để điều khiển chức năng hay submit: "sub\<attributeName>"
- Nếu thuộc tính thỏa mãn nhiều tính chất thì dùng kết hợp nhiều tiền tố: "inoutsub\<attributeName>"
  - _Ví dụ_: Dropdown menu thì vừa là in, vừa là out và vừa là sub vì dropdown menu sẽ phải lấy dữ liệu từ database để hiển thị lên màn hình (out), và người dùng có thể click để lựa chọn (sub), và người dùng lựa chọn cái gì thì sẽ có dữ liệu gửi về hệ thống (in).

### Bước 3: Trích xuất Control Class (Lớp Điều khiển)

**Mục đích:** Mô hình hóa các thuật toán nghiệp vụ phức tạp thành Control Class (Chú ý phân biệt với Controller) điều phối hoạt động giữa Boundary và Entity.
**Cách làm:**

- Từ các Use Case Specification, tìm ra các quy trình tính toán hoặc nghiệp vụ trung tâm và tạo Control Class.
- Bổ sung các method xử lý vào Control Class

  **Lưu ý:**
- Boundary Class nối với Control Class điều khiển nó bằng association
- Control Class nối với các Entity Class mà nó xử lý cũng bằng association.
  **Naming Convention:**
- **Tên Lớp:** Thường là Cụm động từ chỉ hành động chính của Use Case hoặc [Tên Use Case] + `Control`. Viết theo PascalCase.
  - _Ví dụ:_ `LoginControl`.

## B. Tài liệu tham khảo

1. [Video tổng quan các loại diagram: freeCodeCamp](https://www.youtube.com/watch?v=WnMQ8HlmeXc&t=579s "https://www.youtube.com/watch?v=WnMQ8HlmeXc&t=579s")
2. [Video thầy Hùng](https://www.youtube.com/watch?v=2FpGtTRCsvs&list=PLPUAeQ3rg3bI3dlECrYarT60xS-TUdCIk&index=3 "https://www.youtube.com/watch?v=2FpGtTRCsvs&list=PLPUAeQ3rg3bI3dlECrYarT60xS-TUdCIk&index=3")
3. Sách: Object-Oriented and Classical Software Engineering (2010, McGraw-Hill Education)
4. Note thầy Quảng: Chưa thêm method ở pha Analysis.

## C. Hỏi thầy Quảng

1. Trong trường hợp các Boundary Class có sự tương tác lẫn nhau (Boundary Class này gọi Boundary Class khác) thì bọn em có cần nối những Boundary Class này bằng quan hệ Association không ạ? Bọn em có tham khảo video bài giảng của thầy Hùng trên youtube thì thấy thầy Hùng có nối nhưng trong sách thì không có ví dụ và bọn em đọc sách thì bọn em cũng đang hiểu là Boundary nó chỉ nối với Control chứ nó không nối với nhau. Vậy thì cái nào là chính xác vậy ạ?
2. Khi tham khảo giáo trình Object-Oriented and Classical Software Engineering, bọn em thấy các Control Class ở pha Analysis thường không chứa method nào. Lần trước bọn em lên hỏi một Entity Class kế thừa không có attribute nào thì thầy có góp ý là các không nên để trống mà nên bổ sung method đặc trưng. Vậy ở pha phân tích này, chúng em có nên áp dụng nguyên tắc tương tự để bổ sung các method đặc trưng cho Control Class luôn không ạ, hay là vẫn nên để trống ạ?

**Trả lời:** https://docs.google.com/document/d/1xRt8VXCQNXOh6JvyIU6hB-oFwo3b0QbwfvMxSqbzZd4/edit?usp=sharing
